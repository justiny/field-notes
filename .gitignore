# >>> monday-morning:mm >>>
# .mm/ is local-only in git BY DESIGN — team sharing happens via the Monday
# Morning cloud workspace (Settings → Team visibility). Do not force-add past this.
.mm/
# <<< monday-morning:mm <<<

# local dev tooling — not part of the site or the server
.claude/
.mcp.json

node_modules/
.DS_Store

# never commit a live ledger's temp file (saveBrain writes tmp-then-rename)
*.tmp

